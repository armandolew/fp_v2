= form_for @bottle_type do |f|
  - if @bottle_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@bottle_type.errors.count, "error")} prohibited this bottle_type from being saved:"
      %ul
        - @bottle_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
