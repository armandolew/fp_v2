= form_for @family_type do |f|
  - if @family_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@family_type.errors.count, "error")} prohibited this family_type from being saved:"
      %ul
        - @family_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
